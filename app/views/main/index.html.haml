.main-content.container.medium.mt-5
  .top-teams
    - if active_match?
      - if @match.content_match?
        = render partial: 'main_content'
      - elsif @match.project_match?
        = render partial: 'main_project'
    - else
      = render partial: 'main_winners'
