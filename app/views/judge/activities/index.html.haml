%h1 
  = t('activities.all')
%br
%table
  %thead
    %tr
      %th 
        = t('labels.name')
      %th
        = t('labels.type')
      %th
        = t('labels.location')
      %th 
        = t('labels.english')
      %th 
        = t('activities.status')
    %tbody
      - @activities&.each do |activity|
        %tr
          %td
            = link_to activity.name, activity_path(activity)
          %td
            = activity.activity_type
          %td
            = activity.locations.first&.name
          %td
            = activity.english ? "Sí": "No"
          %td 
            = activity.status 
